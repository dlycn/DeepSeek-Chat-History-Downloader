[project]
name = "aichat-history-dl"
version = "0.1.0"
description = "Download DeepSeek chat history to a local JSON as an independent script (users should use it by calling the main function)"
readme = "README.md"
authors = [
    { name = "StarsAll_InFrost", email = "dlycn749007@outlook.com" }
]
requires-python = ">=3.10"
dependencies = [
    "requests>=2.28",
]

[build-system]
requires = ["uv_build>=0.12.9,<0.13.0"]
build-backend = "uv_build"